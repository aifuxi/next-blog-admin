import { POST_TYPE_ENUM } from '@/features/common/types';

export const typeMap = {
  [POST_TYPE_ENUM.ORIGINAL]: '原创',
  [POST_TYPE_ENUM.TRANSLATION]: '翻译',
  [POST_TYPE_ENUM.TRANSSHIPMENT]: '转载',
};
